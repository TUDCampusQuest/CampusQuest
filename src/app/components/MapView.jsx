'use client';

import { Box } from '@mui/material';

export default function MapView({ onSelectLocation }) {
  return (
    <Box
      sx={{
        height: '100%',
        width: '100%',
        backgroundColor: '#f0f0f0',
      }}
    >
      {/* Map will go here */}
    </Box>
  );
}
